mode: ContinuousDeployment

next-version: 1.1.0

branches:
  main:
    regex: ^main$
    increment: patch

  feature:
    regex: ^features?[/-]
    increment: minor

  hotfix:
    regex: ^hotfix(es)?[/-]
    increment: patch

mode: ContinuousDelivery

branches:
  main:
    regex: ^main$
    increment: patch

  feature:
    regex: ^features?[/-]
    increment: minor

  hotfix:
    regex: ^hotfix(es)?[/-]
    increment: patch

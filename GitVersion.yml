mode: ContinuousDeployment

next-version: 2.0.0

branches:
  main:
    regex: ^main$
    increment: patch

  feature:
    regex: ^features?[/-]
    increment: minor

  hotfix:
    regex: ^hotfix(es)?[/-]
    increment: patch
